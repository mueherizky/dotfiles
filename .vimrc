" enable line num
set number 

" enable syntax highlighting
syntax on

" set tab or identation (space to tab)
set tabstop=2

" enable auto indent
set autoindent

" tab to space (overwrite)
set expandtab

" remove mutliple spaces with single backspace
set softtabstop=2

" enable highlight current line (underline)
set cursorline


